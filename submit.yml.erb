---
<%-
 num_cores = bc_num_slots.blank? ? 1 : bc_num_slots.to_i
-%>
batch_connect:
  template: "basic"
script:
  native:
    - "-n"
    - <%= num_cores %>
    - "-R"
    - "span[ptile=<%= num_cores %>]"
